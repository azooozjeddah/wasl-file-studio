# Production E2E Notes

## Target

- Production URL: `https://waslfile-b7bks7br.manus.space/`
- Production title at test start: `وَصل للملفات | أدوات ملفات تعمل على جهازك`
- Test browser: Chromium sandbox browser. A separate Playwright Firefox session was used earlier for local/development verification; Safari and physical Android are unavailable in this environment.

## Published catalog observed from the production homepage

| Family | Published count | Representative routes |
|---|---:|---|
| PDF | 19 | `/merge-pdf`, `/split-pdf`, `/pdf-to-jpg`, `/protect-pdf` |
| Images | 5 | `/convert-image`, `/compress-image`, `/resize-image`, `/rotate-image` |
| Documents | 6 | `/word-to-pdf`, `/pdf-to-word`, `/txt-to-pdf` |
| OCR | 1 | `/ocr` |
| Audio | 4 | `/convert-audio`, `/trim-audio`, `/merge-audio` |
| Video | 7 | `/video-to-mp3`, `/mp4-to-webm`, `/compress-video`, `/extract-frame` |

## Public-production observations

- Arabic RTL homepage loaded successfully and listed every expected tool category and route.
- The production homepage includes language toggle, dark-mode toggle, public admin link, CTA links, privacy/FAQ anchors, canonical public pages, and direct tool links.
- Local tool copy promises client-side processing; network behavior will be verified per selected tool using browser request inspection during this test run.
- Language toggle changed the complete primary navigation, CTA, tool catalog, privacy copy, and FAQ from Arabic RTL to English with no console output.
- Dark-mode toggle changed the published homepage to a readable dark palette while preserving visible navigation, CTAs, file-intake control, and catalog tabs.

## Merge PDF production test

- `/merge-pdf` loaded with the expected local-processing label, multiple-PDF intake, actionable process control, safety copy, and no JavaScript console output before processing.
- The page implements its native file input as a hidden control inside the custom intake button. Browser automation cannot target that hidden native field directly, so the field was temporarily made visible only in the test DOM to exercise the exact same upload handler; no source or deployed code was changed.
- Fixtures were copied to `/home/ubuntu/wasl-e2e-fixtures/` because browser uploads are restricted to paths below `/home/ubuntu/`. This is a harness restriction, not a production application failure.
- A separate Playwright production session loaded both `/` and `/merge-pdf` with the expected Arabic page titles, confirming that the public deployment is independently reachable beyond the initial browser session.
- In the separate session, activating the visible production button `اسحب الملف هنا أو اختره من جهازك` opened the native browser file chooser exactly as expected. The first automated upload was rejected solely because that browser harness permits fixture paths only below `/opt/.manus/current`; the existing source fixture path will be used for the retry.
- Retrying with `/opt/.manus/current/wasl-test-fixtures/sample-a.pdf` and `sample-b.pdf` succeeded through the natural chooser. The UI reported two selected files totaling 4.4 KB, exposed per-file queue controls and enabled `ابدأ المعالجة` without any source or DOM modification.
- Processing those files on production completed successfully and presented `sample-a-merged.pdf` (1.8 KB) as a downloadable result. The output preview in the browser showed two page thumbnails; the independent Chromium execution also rendered the first page content and both thumbnails.
- Production network inspection recorded only catalog retrieval plus anonymous telemetry during local merge. The examined telemetry bodies were `tool_open` with tool/locale/platform/browser fields and `process_start` with only `toolSlug` and a size bucket (`<1MB`); neither included a filename, file bytes, MIME payload, or uploaded file URL.

## Homepage, responsive, and SEO test

- A fresh automated session reached the published homepage with title `وَصل للملفات | أدوات ملفات تعمل على جهازك` and Arabic content. The initial metadata probe used an invalid browser-selector literal while reading Open Graph tags; this is an automation query error, not a browser-console error emitted by the production application. The corrected metadata check follows.
- The corrected production metadata probe found `lang="ar"`, `dir="rtl"`, a non-empty Arabic description, matching Open Graph title/description, and one JSON-LD block. It also found a real SEO defect: the production homepage canonical resolved to `https://wasl-file-studio.manus.space/`, not the active published domain.
- The hard-coded historical canonical in `client/index.html` was replaced with the root-relative value `/`, and a Vitest regression test now ensures the template cannot contain that historical domain. The focused test passed. The current preview resolves that link to its own active origin; this correction will require the next user-initiated Publish action before it can be re-verified on production.
- Production homepage testing has been restarted at a 390 × 844 viewport to simulate an iPhone-class layout. This verifies responsive web behavior in Firefox automation; it is not a claim of execution on physical iPhone, Android hardware, or Safari.
- The original production mobile snapshot exposed a reproducible accessibility issue: the OCR, audio, and video category tabs had negative horizontal positions and therefore were clipped outside the 390 px viewport. The mobile CSS was changed to wrap category tabs into a two-column grid. At the same 390 × 844 preview size, all six links now occupy three visible rows with x coordinates between 12 and 378 px. This must still be re-tested after the next Publish action.

## Image batch production test

- The test viewport was restored to 1366 × 900 and the published `/convert-image` route loaded with the expected Arabic title `تحويل الصور | وَصل للملفات`.
- The production image converter exposed JPG, PNG, and WebP support, output-format and quality controls, and a disabled process control until a file is selected. Activating the visible file-intake button opened the browser-native file chooser successfully.
- The converter accepted three PNG inputs (39.2 KB total), enabled processing, and completed the batch. The result controls exposed three individual `تنزيل` buttons plus `تنزيل الكل ZIP`, alongside `بدء عملية جديدة`; this proves the published UI recognizes a multi-result package.
- The three generated WebP outputs had distinct, expected names and reduced sizes: `sample-a-convert-image.webp` (532 B), `sample-b-convert-image.webp` (9.1 KB), and `ocr-english-convert-image.webp` (9.1 KB). Clicking the production ZIP button downloaded `wasl-results.zip`; archive inspection confirmed it contains exactly those three files with the reported byte lengths.

## PDF split production test

- A three-page fixture (`e2e-multipage.pdf`, 4.5 KB) was prepared from the supplied PDF samples. The published `/split-pdf` route opened with a native single-file picker and a range textbox accepting values such as `1-3,5`.
- The three-page PDF was accepted through the natural production file chooser (shown as one selected 4.4 KB file), and enabled the split action while leaving the page-range setting editable.
- Entering page range `2` produced `e2e-multipage-page-2.pdf` (937 B). The file was downloaded through the production UI and `pdfinfo` confirmed a valid PDF 1.7 with exactly one A4 page, establishing that the selected middle page was extracted rather than merely a placeholder file.

## OCR production test

- The published OCR tool advertises local Arabic/English recognition for JPG, PNG, and PDF inputs, with TXT and DOCX outputs. It exposes a language selector with clear first-use language-download notice and labels its current mode as local, with server-ready handling documented separately.
- The Arabic fixture was accepted through the native production picker and OCR was explicitly set to `ara`. The completed result state appeared after local processing and exposed a ZIP download plus an output group for the extracted artifacts; detailed text-preview and file-output verification follows.
- Production OCR then exposed a reproducible defect: the supplied, visually clear Arabic fixture completed with the preview `لم يتعرف OCR على نص قابل للاستخراج من هذه العينة.` and a zero-byte TXT. No production application console error accompanied the failure. The engine now uses automatic page segmentation first and retries blank passes with `SINGLE_LINE`; the new regression test and all OCR unit tests passed. A live preview retest is in progress before this change can be published and re-verified on production.
- The actual preview retest succeeded: the previous blank Arabic fixture now produced non-empty text beginning `وصل ملفات عربي`. Its final digits were imperfect (`٠١1771` rather than the source’s `٢٠٢٦`), which is a truthful OCR quality limitation; the decisive zero-byte/empty-output defect was resolved in the preview. The Tesseract `Estimating resolution` console entry is emitted by the underlying OCR WebAssembly engine and not by Wasl application code.
- A separate production OCR session has been opened for the English fixture. The same local-only language selector and native multi-file intake are present; English result verification is next.
- The English fixture was uploaded through the native production picker, OCR was set to `eng`, and the local output preview exactly returned `WASL OCR ENGLISH 2026`. This confirms the current production engine recognizes the supplied English sample correctly.

## Document conversion production test

- The published Word-to-PDF page loaded with its disclosed best-effort local rendering limitation, a native DOCX-only picker, and the expected local/server-ready processing label.
- The production Word-to-PDF conversion accepted `sample-word.docx` (8.5 KB), produced `sample-word-rendered.pdf` (72.4 KB), and downloaded it successfully. Independent PDF inspection confirmed one valid A4 PDF page. Text extraction is intentionally empty because this best-effort local path rasterizes the rendered document; visual inspection confirmed the output page contains the English heading, Arabic heading, and supporting paragraph from the DOCX fixture.
- The published PDF-to-Word page loaded with its explicit text-extraction limitation for scanned PDFs, a native single-PDF picker, and local/server-ready processing disclosure. A text-based fixture will now be used to validate the editable DOCX output.
- The native picker accepted `text-based.pdf` (3.2 KB) and enabled the production conversion action. The selected input is a text-based PDF, so successful conversion should yield a non-empty editable document rather than invoke the scanned-PDF rejection path.
- Converting `text-based.pdf` produced `text-based-editable.docx` (8.3 KB) and a companion output package. The DOCX downloaded successfully; its WordprocessingML text nodes contain `WASL TEXT PDF 2026 Editable text extraction verification.`, confirming a non-empty editable text result rather than a static placeholder.

## Media production test

- The production audio converter opened with the expected local/server-ready disclosure, native batch intake, supported MP3/WAV/M4A/AAC/OGG formats, and a bitrate control that remains disabled until media is selected.
- Production WAV conversion accepted the 15.7 KB fixture and began at 3%, while the core JS and WASM downloads returned HTTP 200. The progress did not advance after repeated checks; cancellation returned the workspace to a usable retry state. The occurrence is reproducible and is tracked as a media-engine defect.
- The first diagnosis found that the core was requested from the ESM bundle. Switching to UMD exposed a `NetworkError` when an explicit `ffmpeg-core.worker.js` URL was added; the official package manifest confirms neither UMD nor ESM publishes that file. The unsupported explicit worker URL was removed and the standard UMD configuration retained. The loader-configuration regression test passes, but the actual preview WAV conversion still shows the generic failure state; no fabricated media success has been recorded.
- Further diagnosis proved that `crossOriginIsolated` was false and `SharedArrayBuffer` was unavailable. The server now sends COOP `same-origin` and COEP `require-corp`; a fresh preview has `crossOriginIsolated: true` and `SharedArrayBuffer: function`. The third-party analytics script was removed because it was blocked by the required COEP policy; anonymous same-origin telemetry remains active.
- FFmpeg load diagnostics then identified an incompatible Core request. The installed `@ffmpeg/ffmpeg` 0.12.15 worker expects Core 0.12.9; the loader now uses the matching UMD Core directly, rather than Core 0.12.10 or an invalid worker URL. The targeted configuration and isolation tests pass. A live preview converted `sample.wav` (15.7 KB) to `sample-converted.mp3` (10.2 KB), and the downloaded file independently identifies as MPEG Layer III, 128 kbps, 8 kHz mono. Production remains on the prior release until the next successful publish.

## Local code, signature, and integrity additions (pending publication)

- The current development build adds a local QR generator for URL, text, phone, email, Wi-Fi, SMS, WhatsApp, and vCard payloads. It visibly renders QR output and exposes PNG, SVG, PDF, copy, and print controls. Unit tests cover payload generation and barcode check-digit validation.
- A local QR reader accepts image files and provides permission-gated camera scanning. A local barcode generator visibly renders Code 128, Code 39, EAN-13, EAN-8, and UPC-A output with format validation and PNG, SVG, PDF, copy, and print controls. Desktop and 390 px viewport screenshots verified their responsive layouts.
- A visual-signature PDF workflow now supports drawn, typed, and image signatures plus text/date stamps on selected preview pages. It clearly states that it is not a certified digital signature. Its local PDF engine test created a valid two-page signed output without rasterizing its pages.
- A local file-integrity tool computes SHA-256 and SHA-512 hashes, supports an expected-hash comparison, copy, and TXT export. Its SHA-256 code path was tested against the known `abc` digest.
- The current environment could not launch the independent Firefox Playwright browser because its configured binary is absent. New interactive upload/download E2E steps therefore remain pending; they are not reported as production success. Full project tests passed 56 tests in 26 files, and the memory-limited production build completed successfully.
- FFmpeg-dependent audio and video transformations are now visibly labelled experimental pending a fresh production recheck. Browser-native audio/video metadata and frame extraction remain presented as locally ready; Word/PDF text-conversion and OCR are labelled as needing improvement so no unverified quality is implied.
- Immediately after checkpoint `0c638cd8`, production homepage requests (including a cache-busting query) still returned the previous catalog with six categories and no QR, Barcode, signature, or utility entries. The public `__manus__/version.json` route resolves through the application’s 404 page and cannot be used to inspect deployment propagation. New production workflows are therefore pending until the published catalog changes; no new tool was falsely marked as production-tested.
- After publication propagation completed, the production homepage exposed nine categories, including `QR وBarcode`, `توقيع PDF`, and `أدوات مساعدة`. It listed the QR generator, QR reader, barcode generator, visual PDF signer, and file-integrity checker with `جاهزة محليًا` status. Word/PDF and OCR now show `يحتاج تحسينًا`, while FFmpeg-dependent audio/video transformations show `تجريبي`; native metadata and frame tools remain locally ready. This confirms the catalog, readiness labelling, and homepage prioritization changes reached production.
- Production QR generator opened with local/private disclosure, QR-type choices, live preview, size/error-correction/color controls, and PNG/SVG/PDF/copy/print actions. Entering `https://example.com/wasl-e2e` visibly regenerated the QR preview. PNG export completed as `wasl-qr.png`; browser download history confirms the file originated from the Wasl production domain.
- Production Barcode generator opened with Code 128, Code 39, EAN-13, EAN-8, and UPC-A choices, local validation guidance, a visual preview, and PNG/SVG/PDF/copy/print controls. Selecting EAN-13 correctly rejected the prior non-numeric default. Entering the 12-digit value `629123456789` generated the scannable EAN-13 `6291234567894` and visibly rendered its barcode. PNG export was triggered; download confirmation follows.
- Browser download history confirms `wasl-barcode.png` was exported from the Wasl production domain alongside the QR PNG.
- Production file-integrity testing uploaded a 14-byte text fixture through the normal local input handler (temporarily exposed only for test automation) and calculated `SHA-256 = 1d84800cc9b6cf9a8c629d383334f0eb915a72ffb26524d43df015e262c7b6ce`. An independent sandbox `sha256sum` calculation returned the identical digest. No server-upload endpoint was involved in this local calculation.
- Production visual-PDF-signing initially failed while rasterising a two-page fixture with `i.createElement is not a function`. The affected preview was replaced with a native local PDF object preview and a local page count. After the corrected version propagated, the same two-page fixture displayed both page selectors and page 1 content, accepted a typed visual signature, exposed size/rotation/opacity controls, and downloaded `sign-fixture-signed.pdf` from the Wasl production domain. The page explicitly states that this is a visual signature rather than a legally certified digital signature.
- A project-level daily checkpoint workflow is active under the title `Wasl daily snapshot 06:00 KSA`. It triggers at 06:00 Asia/Riyadh (`0 0 3 * * *` UTC cron) and its scope explicitly excludes user-file content, database records, credentials, and secrets.
- A short 160×90 WebM fixture was generated directly in the browser with `MediaRecorder`, then uploaded to the production `معلومات الفيديو` tool. The local result `wasl-e2e-info.txt` downloaded successfully and reported the expected type (`video/webm`), size (13,511 bytes), dimensions (160×90), and duration (0.89 seconds). This confirms the native metadata workflow; FFmpeg-dependent video transformations remain explicitly labelled experimental pending separate compatibility evidence.
- The same WebM fixture was accepted by `استخراج إطار` at the zero-second starting point. Production showed a local PNG preview and generated `wasl-e2e-frame.png`, reducing the 13.2 KB source to a 3.6 KB output; browser download history confirmed the PNG was downloaded from the Wasl production domain.
- A one-second 8 kHz WAV fixture was generated locally in the browser and tested against production `معلومات الصوت`. Its downloaded result correctly reported `audio/wav`, 16,044 bytes, and `durationSeconds: 1`. These native audio/video inspection and frame workflows are confirmed locally functional; FFmpeg conversion, trimming, merge, and compression paths remain visibly experimental rather than being overstated as production-ready.
- The same one-second WAV was submitted to production `تحويل الصوت`. The FFmpeg-based conversion displayed cancellable local processing but remained at 3% after a short wait instead of producing a result. Pressing `إلغاء` immediately restored the ready state and displayed the truthful cancellation notice. This confirms cancellation behavior while preserving the experimental classification for this incomplete compatibility path.
- After the local FFmpeg Core/WASM/worker bundling did not change sandbox-browser conversion compatibility, the published media pages now display an explicit experimental-compatibility notice. The same WAV conversion returns from 3% after exactly 15 seconds to a retryable state with the message `انتهت مهلة تحميل محرك الوسائط المحلي. جرّب متصفحًا حديثًا أو أعد المحاولة.` This is a verified containment improvement, not a claim that WAV-to-MP3 succeeded in this browser.
- Production QR reader accepted `wasl-qr.png`, a PNG generated by the production QR generator, through the native local image input. It decoded the exact source payload `https://example.com/wasl-e2e` and exposed the copy action, proving the generate-to-read workflow remains in-browser and reversible without a server-side upload.
- Switching the same QR-reader session to English translated navigation, instructions, buttons, and decoded-content labels while preserving the decoded URL. In the English Barcode generator, EAN-13 input `629123456789` generated and visibly rendered the correct scannable value `6291234567894`; the local validation behavior is consistent across the two interface languages.
- Production canonical verification now passes: homepage resolves to `https://waslfile-b7bks7br.manus.space/`, privacy to `https://waslfile-b7bks7br.manus.space/privacy`, and QR generator to `https://waslfile-b7bks7br.manus.space/qr-generator`. The historical project-domain canonical is no longer emitted by these public page types.
- Re-testing the Arabic OCR fixture on production with `ara` completed successfully. The local preview is non-empty (`وصل ملفات عربي ‎٠١1771‏`), and the production page exposes a 41-byte TXT plus DOCX and PDF outputs. The year digits remain an acknowledged OCR-recognition limitation, but the prior zero-byte/empty-output defect is resolved on the active domain.
- Production access control was checked without an authenticated session: `/admin` shows only the role-gated OAuth sign-in screen and exposes no dashboard data; `/first-admin` resolves to `إعداد التطوير غير متاح` and states the route is closed outside development. The development-only bootstrap does not create production credentials or payment access.
- The original static SEO documents continued to serve the historical domain even after source changes, so the application now supplies `robots.txt` and `sitemap.xml` before static/fallback middleware. Production verification after release `73ff11cb` confirms that robots points to `https://waslfile-b7bks7br.manus.space/sitemap.xml` and every listed sitemap location uses the active production domain.
- A fresh production homepage load produced no browser-console output. The HTML template no longer references the external analytics endpoint, while the app’s first-party anonymous event path remains available; this avoids the earlier cross-origin-embedder-policy block without uploading local file content.
- A fresh 390 × 844 development viewport capture includes all expanded category links—PDF, images, documents, OCR, QR/Barcode, signing, utilities, audio, and video—in wrapped rows with no clipped controls. This verifies the CSS repair locally after the category expansion; production remains subject to the same published CSS and will receive a separate routed check when a mobile production viewport is available.
- The working production media path is now a same-origin Express-served **classic worker** with the installed UMD Core and WASM. Direct instrumentation measured a core startup of roughly 24.2 seconds, so the load budget was raised to 45 seconds. The published WAV-to-MP3 workflow then completed locally: `wasl-e2e-converted.mp3` was 10.2 KB from the 15.7 KB WAV fixture, and an independent file inspection confirmed MPEG Layer III, 128 kbps, 8 kHz mono. The experimental label remains for media transformation modes not yet tested, rather than for this confirmed audio conversion.
- Production `watermark-pdf` accepted a two-page PDF and generated `sign-fixture-watermarked.pdf` at 1.8 KB from a 1.1 KB input, exposing the local Download action. The sandbox browser click reached the download control, but its download bridge did not expose a file in `/home/ubuntu/Downloads` for `pdfinfo`; this is recorded as a harness-artifact limitation, so the independent file-open portion of this one acceptance row remains unconfirmed rather than being reported as a complete pass.
- Production `rotate-image` accepted the Arabic PNG fixture (14.7 KB), converted it locally with the configured 270° rotation and WebP output, and rendered a 6.6 KB non-empty result preview named `ocr-arabic-rotate-image.webp`. The download control was activated once; as with the watermark file, the sandbox download bridge did not expose the browser download in `/home/ubuntu/Downloads`, so the independent on-disk inspection is recorded as unavailable rather than repeated.
- Production `txt-to-docx` accepted a 14-byte local text fixture and produced `hash-fixture-converted.docx` (8.3 KB) plus a 13-byte extracted TXT result. The DOCX download reached `/home/ubuntu/Downloads`, proving a real archive rather than a button-only result. The one independent `word/document.xml` text-node extraction did not yield printable text from this minimal fixture, so this specific text-content assertion is recorded as inconclusive and the tool remains in the evidence-based `needs improvement` category rather than being upgraded.
- A fresh unauthenticated production visit to `/admin?acceptance=final` shows only the role-gated OAuth sign-in screen and does not expose dashboard data, catalog mutation controls, job queues, or user records.
- Production `delete-pdf-pages` accepted `sign-fixture.pdf`, removed page 2, and produced the visible non-empty `sign-fixture-pages-removed.pdf` at 889 B from the 1.1 KB two-page input. The Download control was activated once. The sandbox bridge did not expose this PDF in `/home/ubuntu/Downloads` for `pdfinfo`, so independent page-count inspection is unavailable and no retry will be made in this phase.
- Production `pdf-metadata` accepted the same PDF and generated `sign-fixture-metadata.txt` at 153 B through the local result panel. Its Download control was activated once, but the sandbox bridge did not expose that text file in `/home/ubuntu/Downloads` for content inspection. The operation is therefore logged as a visible non-empty local result with independent-download verification unavailable, with no retry.
- Production `reorder-pdf-pages` accepted the two-page fixture, applied order `2,1`, generated a visible `sign-fixture-reordered.pdf` at 1.1 KB, and had its Download control activated once. The sandbox bridge limitation remains the only obstacle to independent PDF-order inspection; no retry or repair is scheduled.
- Production `compress-image` accepted `ocr-arabic.png` (14.7 KB), re-encoded it locally to a visible WebP result of 6.7 KB at 82% quality, and activated Download once. The sandbox download bridge did not expose this WebP for `file` inspection, so the operation is logged as a visible smaller local result with independent download verification unavailable and no retry.
- Production `resize-image` accepted the same PNG, applied a 200-pixel width setting, produced the visible `ocr-arabic-resize-image.webp` at 3.5 KB, and activated Download once. The sandbox bridge did not expose the image for independent dimension inspection; this is recorded without retry.
- Production `crop-image` accepted the same PNG with its local crop box, produced the visible `ocr-arabic-crop-image.webp` at 1.6 KB, and activated Download once. The sandbox bridge did not expose the output for independent pixel-dimension inspection; this is recorded without retry.
- Production `txt-to-pdf` accepted a 14-byte text fixture, produced the visible `hash-fixture-converted.pdf` at 3.1 KB, and activated Download once. The sandbox bridge did not expose this PDF for independent inspection; the local non-empty output and download action are recorded without retry.
- Production `qr-generator` accepted `https://example.com/wasl-final-acceptance`, visibly regenerated the 320 px local QR, and activated its PNG export once. Earlier production evidence already confirmed that a downloaded Wasl QR could be read back by the local QR reader; no repeat read is scheduled.
- Production `page-numbers-pdf` accepted a two-page PDF fixture, generated the visible `sign-fixture-numbered.pdf` at 1.6 KB, and activated Download once. The PDF-viewer bridge did not expose an independently downloaded file for inspection; this is recorded as a non-empty local result without retry.
- Production `rotate-pdf` accepted the same two-page fixture at 90°, generated the visible `sign-fixture-rotated.pdf` at 1.1 KB, and activated Download once. The PDF-viewer bridge did not expose an independently downloaded file for rotation inspection; this is recorded as a non-empty local result without retry.
- Production `compress-pdf` was attempted once on the 1.1 KB two-page fixture and rejected it with the explicit local safeguard `لم يحقق هذا الملف انخفاضًا في الحجم بالإعدادات الحالية. جرّب جودة أقل أو احتفظ بالملف الأصلي.` No retry or quality adjustment was made, as the source was already compact and the tool correctly avoided delivering a larger result.
- Production `pdf-to-png` accepted the two-page PDF fixture and generated two visible PNG outputs (37.1 KB and 37.5 KB) plus an activated ZIP download. No additional re-run was performed.
- Production `html-to-pdf` accepted a 220-byte Arabic/English HTML fixture and generated the visible local result `acceptance-converted.pdf` (3.1 KB); its download button was activated once. No further attempt was made.
- Production `rtf-to-pdf` accepted a 107-byte RTF fixture and generated the visible local result `acceptance-converted.pdf` (3.1 KB); its download button was activated once. No further attempt was made.
- After the updated production bundle propagated, a video-only WebM now yields the clear local message `لم يتم العثور على مسار صوت صالح في الفيديو. اختر فيديو يحتوي صوتًا ثم أعد المحاولة.` rather than `FS error`. Separately, the same engine successfully converted a WebM fixture containing an Opus audio track to a valid MP3 and merged two WAV files; the remaining video conversion, trim, and compression modes retain their experimental status pending dedicated checks.
- The same WAV fixture completed production `قص الصوت`, producing `wasl-e2e-trimmed.mp3` at 10.2 KB; an independent inspection confirmed MPEG Layer III, 128 kbps, 8 kHz mono. A video-only WebM fixture correctly failed extraction because it carried no audio stream (the generic `FS error` will receive a clearer diagnostic separately). A second one-second WebM fixture with an Opus audio track was generated locally in-browser; production `Video to MP3` completed and downloaded `wasl-e2e-audio-audio.mp3` (15.8 KB), independently identified as valid MPEG Layer III, 128 kbps, 48 kHz stereo.
