import PublicLayout from "@/components/PublicLayout";
import ToolWorkspace from "@/components/ToolWorkspace";
import { useLocale } from "@/contexts/LocaleContext";
import { findTool } from "@/lib/tools";
import { ArrowLeft, CheckCircle2, FileSearch, LockKeyhole } from "lucide-react";
import { Link, useLocation } from "wouter";
import { useEffect } from "react";
import NotFound from "./NotFound";

export default function ToolPage() { const [location] = useLocation(); const tool = findTool(location.slice(1)); const { isArabic, t } = useLocale(); useEffect(() => { if (!tool) return; const title = isArabic ? `${tool.labelAr} | وَصل للملفات` : `${tool.labelEn} | Wasl File Studio`; document.title = title; const description = isArabic ? tool.descriptionAr : tool.descriptionEn; document.querySelector('meta[name="description"]')?.setAttribute("content", description); document.querySelector('link[rel="canonical"]')?.setAttribute("href", `${window.location.origin}${location}`); }, [tool, isArabic, location]); if (!tool) return <NotFound/>; const Icon = tool.icon; const name = isArabic ? tool.labelAr : tool.labelEn; const localAnswer = tool.local ? t("تعمل هذه الأداة محليًا داخل متصفحك ولا تُرفع ملفاتها من أجل التحويل.", "This tool runs locally in your browser and does not upload files for conversion.") : t("يعرض الموقع بوضوح طريقة المعالجة قبل بدء الاستخدام.", "The site clearly explains processing mode before use."); const faq = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: [{ "@type": "Question", name: t(`كيف تعمل أداة ${name}؟`, `How does ${name} work?`), acceptedAnswer: { "@type": "Answer", text: t("اختر الملفات، عدّل الإعدادات المتاحة، ثم نزّل النتيجة التي ينشئها المتصفح.", "Choose files, adjust available settings, then download the result generated by the browser.") } }, { "@type": "Question", name: t("هل الملف آمن؟", "Is the file safe?"), acceptedAnswer: { "@type": "Answer", text: localAnswer } }, { "@type": "Question", name: t("ما الصيغ المدعومة؟", "Which formats are supported?"), acceptedAnswer: { "@type": "Answer", text: tool.formats.join(", ") } }] }; return <PublicLayout><main className="tool-page"><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(faq) }}/><div className="container"><div className="tool-breadcrumb"><Link href="/">{t("الرئيسية", "Home")}</Link><span>/</span><Link href="/#tools">{t("الأدوات", "Tools")}</Link><span>/</span><b>{name}</b></div><section className="tool-hero"><span className={`tool-icon tool-${tool.category}`}><Icon size={22}/></span><div><span className="section-eyebrow">{tool.category.toUpperCase()} · {tool.local ? t("محلي", "LOCAL") : t("قريبًا", "SOON")}</span><h1>{name}</h1><p>{isArabic ? tool.descriptionAr : tool.descriptionEn}</p></div></section><ToolWorkspace tool={tool}/><section className="tool-explainer"><div><FileSearch size={20}/><h2>{t("كيف تعمل الأداة؟", "How this tool works")}</h2><p>{t("اختر الملف، اضبط الإعدادات، ثم ابدأ المعالجة. النتيجة تُجهز داخل المتصفح لتتمكن من تنزيلها فورًا.", "Choose your file, configure the settings, then process. Your result is prepared inside the browser for immediate download.")}</p></div><div><LockKeyhole size={20}/><h2>{t("هل الملف آمن؟", "Is the file safe?")}</h2><p>{localAnswer}</p></div><div><CheckCircle2 size={20}/><h2>{t("الصيغ المدعومة", "Supported formats")}</h2><p>{tool.formats.join(" · ")}</p></div></section></div></main></PublicLayout>; }
