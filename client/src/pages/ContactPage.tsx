import PublicLayout from "@/components/PublicLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { useLocale } from "@/contexts/LocaleContext";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, Mail, MessageSquareText, ShieldCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

export default function ContactPage() {
  const { t } = useLocale(); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [subject, setSubject] = useState(""); const [message, setMessage] = useState("");
  const submit = trpc.contact.submit.useMutation({ onSuccess: () => { setName(""); setEmail(""); setSubject(""); setMessage(""); toast.success(t("تم إرسال رسالتك إلى فريق وَصل.", "Your message was sent to Wasl.")); }, onError: error => toast.error(error.message) });
  const onSubmit = (event: FormEvent) => { event.preventDefault(); submit.mutate({ name, email, subject, message }); };
  return <PublicLayout><main className="container py-10 md:py-14" dir="rtl"><header className="mx-auto max-w-3xl text-center"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-violet-100 text-violet-700 dark:bg-violet-500/15"><MessageSquareText size={28}/></span><h1 className="mt-5 text-3xl font-black">{t("تواصل معنا", "Contact us")}</h1><p className="mt-3 text-muted-foreground">{t("أرسل سؤالًا أو اقتراحًا أو مشكلة واجهتك في المنصة. اكتب الوصف فقط ولا ترسل ملفات أو كلمات مرور أو أي بيانات حساسة.", "Send a question, suggestion, or issue. Describe it only; do not send files, passwords, or sensitive data.")}</p></header><div className="mx-auto mt-8 grid max-w-5xl gap-6 lg:grid-cols-[1.05fr_.75fr]"><form onSubmit={onSubmit} className="rounded-3xl border bg-card p-6 shadow-sm md:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="grid gap-2"><Label>{t("الاسم", "Name")}</Label><Input value={name} onChange={event => setName(event.target.value)} required minLength={2} maxLength={120} placeholder={t("اسمك", "Your name")}/></label><label className="grid gap-2"><Label>{t("البريد الإلكتروني", "Email")}</Label><Input type="email" value={email} onChange={event => setEmail(event.target.value)} required maxLength={320} placeholder="name@example.com"/></label></div><label className="mt-5 grid gap-2"><Label>{t("الموضوع", "Subject")}</Label><Input value={subject} onChange={event => setSubject(event.target.value)} required minLength={3} maxLength={180} placeholder={t("مثال: استفسار حول أداة PDF", "Example: question about a PDF tool")}/></label><label className="mt-5 grid gap-2"><Label>{t("الرسالة", "Message")}</Label><Textarea value={message} onChange={event => setMessage(event.target.value)} required minLength={10} maxLength={5000} rows={7} placeholder={t("اكتب التفاصيل التي تساعدنا على فهم طلبك…", "Write the details that help us understand your request…")}/></label><Button className="mt-6" disabled={submit.isPending}>{submit.isPending ? t("جارٍ الإرسال…", "Sending…") : t("إرسال الرسالة", "Send message")}<Mail size={16}/></Button></form><aside className="space-y-4"><div className="rounded-3xl border bg-card p-6"><ShieldCheck className="text-violet-600"/><h2 className="mt-3 font-bold">{t("خصوصيتك أولًا", "Privacy first")}</h2><p className="mt-2 text-sm leading-7 text-muted-foreground">{t("نحفظ نص رسالتك ومعلومات التواصل فقط للرد عليها. لا نطلب رفع ملفاتك إلى صفحة التواصل.", "We store only your message text and contact details to respond. We do not ask you to upload files here.")}</p></div><div className="rounded-3xl border bg-card p-6"><CheckCircle2 className="text-emerald-600"/><h2 className="mt-3 font-bold">{t("ماذا يحدث بعد الإرسال؟", "What happens next?")}</h2><p className="mt-2 text-sm leading-7 text-muted-foreground">{t("تصل الرسالة إلى صندوق رسائل وَصل في لوحة الإدارة حيث يمكن للفريق قراءتها ومتابعتها.", "Your message reaches the Wasl admin inbox where the team can review and follow up.")}</p></div></aside></div></main></PublicLayout>;
}
