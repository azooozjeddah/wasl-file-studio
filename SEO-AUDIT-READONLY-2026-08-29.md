# تقرير تدقيق SEO — Wasl File Studio

**نوع العملية:** تدقيق قراءة فقط، دون تعديل كود التطبيق أو قاعدة البيانات أو الكتالوج أو Registry أو API أو الصلاحيات أو محركات معالجة الملفات.

**تاريخ التدقيق:** 29 أغسطس 2026.

## الخلاصة التنفيذية

الموقع يملك أساسًا جيدًا من ناحية بنية الروابط الدلالية، وجود عناوين `h1` داخل الصفحات العامة، وروابط HTML حقيقية تستخدم `href` مع توجيه History API. كما أن صفحات الأدوات مبنية من سجل موحد، وتشرح طريقة المعالجة والخصوصية، وهذا مناسب لهوية المنتج العربية أولًا.

لكن توجد فجوات حرجة تمنع اعتبار البنية الحالية جاهزة لتنافس SEO على نطاق واسع. أهمها أن `robots.txt` و`sitemap.xml` ومسار canonical ما زالت تشير إلى نطاق Manus الفرعي بدل النطاق النهائي `https://waselfile.com/`. كذلك فإن فهرس sitemap يضم ستة عناوين فقط، ولا يضم `/tools` أو صفحات التصنيفات أو معظم صفحات الأدوات. صفحات التصنيف وفهرس الأدوات وصفحة التواصل لا تضبط metadata مخصصة، بينما صفحات الأدوات تضبط `title` و`description` و`canonical` عبر JavaScript بعد التحميل، ولا تضبط Open Graph أو Twitter metadata المخصصة.

المشكلة التقنية الأهم هي أن الخادم يعيد `index.html` بحالة HTTP `200` للمسارات غير الموجودة، في حين تعرض الواجهة `NotFound` بعد تشغيل JavaScript. هذا يعرّض المسارات العشوائية لخطر **soft 404** ويحتاج معالجة قبل توسيع الفهرسة. كما أن الصفحات المحمية أو الإدارية لا يظهر لها في الشيفرة التي تمت مراجعتها توجيه واضح إلى `noindex`.

## 1. بنية الموقع والصفحات العامة

### الجرد المكتشف

| المجال | النتيجة القراءة فقط |
|---|---|
| تعريفات الأدوات | 66 أداة في `client/src/lib/tools.ts` |
| تصنيفات واجهة الموقع | 9 تصنيفات في `client/src/lib/site-categories.ts` |
| تعريفات Route | 24 تعريفًا في `client/src/App.tsx` |
| صفحة البداية | `/` |
| فهرس الأدوات | `/tools` |
| صفحات التصنيفات | `/tools/:categorySlug`، وتشمل PDF وWord والمستندات وExcel والصور والوسائط وQR والمطورين والبيانات وأدوات أخرى بحسب البيانات المتاحة |
| صفحات الأدوات | `/:toolSlug` مع مسارات صريحة لبعض الأدوات مثل QR وPowerPoint والتوقيع وفحص سلامة الملف |
| صفحات مؤسسية | `/about` و`/contact` و`/terms` و`/privacy` |
| صفحات الحساب والإدارة | `/login` و`/register` و`/dashboard` و`/account/security` و`/admin/*` |
| صفحة 404 | `/404` مع fallback عام في نهاية `Switch` |

الروابط الداخلية الأساسية سليمة من حيث كونها روابط HTML فعلية، وليست روابط hash-only لتحميل صفحات مختلفة. الصفحة الرئيسية تربط إلى الأدوات المميزة وإلى التصنيفات، وفهرس `/tools` يربط إلى صفحات التصنيف، وصفحات التصنيف تربط إلى الأدوات. هذا يقلل خطر orphan pages للأدوات النشطة التي تظهر في الكتالوج. مع ذلك، تعتمد الصفحة الرئيسية وفهرس الأدوات على بيانات الكتالوج العامة وحالة `showOnHome`، ولذلك ينبغي توليد sitemap من نفس مصدر الحالة النشطة بدل الاعتماد على قائمة ثابتة.

توجد فجوة ربط داخلية في الفوتر؛ فهو يربط مباشرة إلى PDF والمستندات والصور وQR، لكنه لا يربط مباشرة إلى Word وExcel والوسائط والمطورين والبيانات. كما أن صفحات الأدوات تربط في breadcrumb إلى `/#tools` بدل رابط directory مباشر إلى `/tools` أو تصنيف مناسب. هذا ليس كسرًا وظيفيًا، لكنه أقل وضوحًا لمحركات البحث والمستخدمين.

### orphan pages وURL structure

المسار الديناميكي `/:toolSlug` يجعل كل أداة معرفة في `tools.ts` مرشحًا لمسار مستقل. الأدوات النشطة المصنفة ترتبط عادة عبر `/tools` ثم التصنيف، لكن الأدوات غير النشطة أو المخفية قد تبقى قابلة للوصول المباشر وتعرض رسالة غير متاحة. يجب التفريق في خطة SEO بين صفحة أداة عامة جاهزة، وصفحة تجريبية أو صيانة، وصفحة محمية تتطلب تسجيل دخول؛ لا ينبغي إدراج الفئات الأخيرة في sitemap أو تقديمها كصفحات بحثية جاهزة.

البنية الحالية قصيرة وواضحة، لكن عدم وجود سياسة صريحة للـtrailing slash والتحويلات يجعل `/path` و`/path/` مرشحين محتملين لنسختين URL ما لم يعالج الخادم ذلك. لم يُثبت من هذا التدقيق وجود redirect موحد لهذه الصيغ. كما لم يُثبت وجود redirect شامل من نطاق Manus الفرعي إلى `waselfile.com`.

## 2. On-page SEO

| نوع الصفحة | title | description | H1 | canonical | OG/Twitter | الملاحظة |
|---|---|---|---|---|---|---|
| Home `/` | موجود في `client/index.html`، ويمكن لإعدادات الكتالوج تغييره بعد التحميل | موجود عالميًا ويمكن تحديثه من إعدادات الموقع | موجود: «ارفع ملفك، وصّل يعرف ماذا يحتاج.» | يبدأ بنطاق Manus ويُعدّل بالـJS إلى origin الحالي | OG أساسي فقط، Twitter غير ظاهر | أساس جيد، لكن canonical الأولي غير متسق مع النطاق النهائي |
| `/tools` | يرث title العام | يرث description العام | موجود | لا يوجد منطق مخصص | غير موجود مخصصًا | يحتاج metadata مستقلة ووصفًا غنيًا لفهرس الأدوات |
| `/tools/:categorySlug` | يرث title العام | يرث description العام | موجود لكل تصنيف صالح | لا يوجد منطق مخصص | غير موجود | فجوة كبيرة لصفحات landing الخاصة بالتصنيفات |
| `/:toolSlug` | يضبطه `ToolPage.tsx` من حقول SEO إن وجدت أو من اسم الأداة | يضبطه من حقول SEO أو الوصف المختصر | موجود باسم الأداة | يضبطه JavaScript بعد التحميل | لا توجد OG/Twitter مخصصة | metadata الأساسية موجودة، لكن المحتوى قصير والرندر client-side |
| `/about`, `/terms`, `/privacy` | يضبطها `StaticPage.tsx` | يضبطها من المحتوى أو fallback | موجود | يضبطه JavaScript | غير موجود | المحتوى الحالي مختصر جدًا، باستثناء أي محتوى مُدار من الكتالوج |
| `/contact` | لا تظهر metadata مخصصة | لا تظهر metadata مخصصة | موجود | لا يظهر منطق مخصص | غير موجود | صفحة عامة، لكنها ليست هدفًا بحثيًا أولويًا |
| `/login`, `/register`, `/dashboard`, `/admin/*` | لا تظهر إدارة metadata مخصصة | لا تظهر إدارة metadata مخصصة | متغير بحسب الشاشة | غير مخصص | غير موجود | ينبغي عادةً حجب صفحات الحساب والإدارة من الفهرسة |
| 404 | صفحة مستقلة، إنجليزية غالبًا | غير مخصص | موجود بحسب العرض | غير مخصص | غير موجود | تحتاج عنوانًا عربيًا و`noindex` أو استجابة 404 حقيقية |

الصفحات العامة تستخدم غالبًا محتوى عربيًا وإنجليزيًا عبر `LocaleContext`، وهو اتجاه مناسب. لكن metadata العالمية في `index.html` لا تحتوي على `og:url` أو `og:image` أو `og:site_name`، ولا تظهر tags من نوع Twitter Card. كذلك لا يوجد في الفحص الحالي `hreflang` أو استراتيجية واضحة لنسخ عربية وإنجليزية مستقلة.

### العناوين والمحتوى

صفحة Home تحتوي H1 واحدًا وعدة H2 واضحة، وفهرس الأدوات يحتوي H1 ثم H2 لكل تصنيف، وصفحة الأداة تحتوي H1 وH2 تفسيرية. هذه بنية دلالية جيدة. المشكلة ليست في غياب العناوين، بل في أن صفحات التصنيفات والأدوات تعتمد على وصف قصير جدًا غالبًا، دون أسئلة شائعة مخصصة فعلية، أمثلة استخدام، شرح للمدخلات والمخرجات، قيود المعالجة، أو روابط موضوعية بين الأدوات ذات العلاقة.

صفحات الأدوات تحقن `FAQPage` JSON-LD من ثلاثة أسئلة. هذا قابل للتحسين فقط إذا كانت الأسئلة والأجوبة ظاهرة للمستخدم ومتطابقة معها. كما توجد FAQ عامة في `index.html`، بينما النص المرئي في Home يعتمد FAQ مُدارة أو fallback مختلفًا جزئيًا؛ يجب منع أي تعارض بين structured data والمحتوى المرئي.

## 3. Technical SEO

### robots.txt وsitemap.xml

`client/public/robots.txt` و`server/seoRoutes.ts` يسمحان بالزحف لجميع المسارات، لكنهما يشيران إلى:

```text
https://waslfile-b7bks7br.manus.space/sitemap.xml
```

كما أن `sitemap.xml` يحتوي ستة عناوين فقط: الصفحة الرئيسية، الخصوصية، من نحن، دمج PDF، ضغط PDF، وتحويل الصور، وكلها على نطاق Manus الفرعي. الدومين النهائي الذي فُحص هو `https://waselfile.com/`، لذلك توجد إشارة canonical/discovery غير متسقة يجب اعتبارها **P0**.

وفق إرشادات Google، يجب أن يحتوي sitemap على URLs مطلقة تمثل النسخ canonical التي يريد الموقع ظهورها، وأن يكون متاحًا من جذر الموقع أو مقدمًا في Search Console [1]. كما أن sitemap إشارة مساعدة وليس ضمانًا للفهرسة [1].

### canonical

`client/index.html` يبدأ بـcanonical ثابت على نطاق Manus ثم ينفذ script يبدله إلى `window.location.origin + window.location.pathname`. هذا يسبب مشكلتين: قيمة HTML الأولية لا توافق النطاق النهائي، وبعض الصفحات مثل `/tools` والتصنيفات لا تملك منطقًا مستقلًا لتعيين canonical بعد تغيّر route. `ToolPage.tsx` و`StaticPage.tsx` يضبطان canonical بعد التحميل، لكن ذلك يظل client-side.

الأفضل مستقبلاً أن يكون canonical هو عنوان نهائي ثابت ومتسق من الخادم أو من metadata route-aware، وألا توجد عدة canonical tags. توضح Google أن canonical إشارة وليست أمرًا ملزمًا، وأن الاتساق بين HTML وsitemap والredirects مهم [2].

### 404 وcrawlability

`server/_core/vite.ts` يمرر كل مسار غير موجود إلى `index.html` بحالة `200`. لذلك أعاد فحص `/nonexistent-seo-audit` حالة `200` مع HTML التطبيق، بينما سيعرض React لاحقًا صفحة NotFound. هذا نمط soft 404 محتمل. توصي Google باستجابات HTTP ذات معنى، وتنبّه إلى خطر soft 404 في تطبيقات SPA؛ يمكن استخدام 404 حقيقية أو `noindex` لصفحة الخطأ [3].

وجود روابط `<a href>` ومسارات History API جيد من ناحية اكتشاف الروابط؛ Google توضح أن الروابط القابلة للاكتشاف تحتاج عناصر anchor مع `href`، وأن استخدام History API أفضل من تحميل صفحات مختلفة عبر fragments [3]. لكن الاعتماد الكامل على JavaScript يعني أن المحتوى والعناوين والـcanonical لا تظهر كلها في HTML الأولي. prerender أو SSR ليس مطلوبًا بالضرورة فورًا، لكنه سيكون ذا أثر كبير على قابلية الزحف وسرعة ظهور المحتوى.

### HTTPS وheaders

فحص الدومين النهائي أظهر HTTPS وHSTS، مع `x-content-type-options: nosniff` وطبقة Cloudflare. هذه نقاط إيجابية. لم يثبت من هذا التدقيق وجود redirect موحد من HTTP إلى HTTPS على كل المسارات، ولذلك ينبغي اختبار ذلك في مرحلة التنفيذ. كما لم يثبت وجود `X-Robots-Tag` أو قواعد noindex خاصة بالمسارات الحساسة.

## 4. Structured Data

الموجود حاليًا:

| المكان | النوع | التقييم |
|---|---|---|
| `client/index.html` | `SoftwareApplication` و`FAQPage` ضمن graph | بداية مناسبة، لكنها تفتقد خصائص تعريفية مفيدة مثل URL والنسخة والجهة المالكة، ويجب مراجعة FAQ مقابل النص المرئي |
| `ToolPage.tsx` | `FAQPage` ديناميكي لكل أداة | مناسب فقط عندما تكون الأسئلة والأجوبة ظاهرة ومحددة فعليًا للصفحة؛ يحتاج اتساقًا واختبارًا لكل locale |
| صفحات التصنيف | لا يظهر Schema مخصص | يمكن دراسة `CollectionPage` أو `ItemList` بشرط أن يعكس الروابط والمحتوى المرئي دون حشو |
| الصفحات الثابتة | لا يظهر Schema مخصص | لا حاجة لإضافة Schema لمجرد الإضافة |

لا توجد في الشيفرة التي تمت مراجعتها بيانات تقييمات أو مراجعات مصطنعة، وهذه نقطة سلامة مهمة. يجب عدم إضافة `Review` أو `AggregateRating` دون بيانات حقيقية قابلة للتحقق. Google توصي بأن تصف البيانات المنظمة المحتوى الموجود للمستخدم، وأن تكون أقل وأكمل أفضل من بيانات كثيرة ناقصة أو غير دقيقة [4]. كما توصي غالبًا بصيغة JSON-LD والتحقق عبر Rich Results Test [4].

### Schema المقترح للدراسة لاحقًا، دون إضافته الآن

يمكن دراسة `WebSite` و`Organization` أو `SoftwareApplication` على Home، و`BreadcrumbList` على صفحات الأدوات والتصنيفات، و`ItemList` على صفحات التصنيفات إذا كانت العناصر ظاهرة فعلًا، و`FAQPage` فقط للأسئلة المرئية المتطابقة. لا أوصي بإضافة `HowTo` أو مراجعات أو تقييمات دون محتوى موثق ومطابق.

## 5. Internal Linking

الهرم الداخلي الحالي هو: Home ←→ `/tools` ←→ `/tools/:categorySlug` ←→ `/:toolSlug`. هذا جيد كبداية. Home يضع مجموعة أدوات مميزة وعددًا من التصنيفات، وفهرس الأدوات يربط إلى جميع التصنيفات المتاحة، والتصنيف يعرض أدواته.

التحسينات الأعلى أثرًا هي إضافة روابط سياقية بين الأدوات المتقاربة، مثل ربط OCR بأدوات الصور وPDF، وربط PDF إلى Word بأدوات OCR، وربط ضغط الصور بتحويل الصور. ينبغي أن تكون الروابط جزءًا من محتوى مرئي مفيد، لا شبكة روابط آلية فقط. كما يُستحسن توسيع روابط الفوتر لتشمل التصنيفات المهمة، وإضافة breadcrumb إلى التصنيف الفعلي بدل `/#tools` فقط.

الأدوات التجريبية أو المتوقفة أو غير المسموحة لا ينبغي إدراجها في sitemap أو في روابط landing SEO الأساسية. أما الأدوات النشطة التي `showOnHome=0` فقد تكون مفيدة في صفحات التصنيف، لكن يجب التأكد من أنها تملك رابطًا داخليًا واحدًا على الأقل إذا كان الهدف فهرستها.

## 6. Performance SEO

التطبيق React SPA مع مكتبات عديدة لمعالجة PDF والصور والصوت والفيديو وOffice. هذا يرفع احتمال تضخم entry bundle، خصوصًا إذا كانت مسارات الأدوات تستورد محركات ثقيلة في المسار العام بدل تحميلها lazy عند فتح الأداة. جرد الأصول المحلي أظهر وجود `dist` بحجم يقارب 7.8 MB ككل، لكنه لا يكفي وحده لتحديد حجم JavaScript المنقول فعليًا لكل صفحة، ولم يكتمل قياس أحجام ملفات الأصول من الدومين النهائي بسبب بطء الاستجابة أثناء محاولة الفحص.

الملاحظات المصدرية الأساسية:

| المؤشر | الحالة الحالية | الإجراء المقترح لاحقًا |
|---|---|---|
| JavaScript | SPA ومكتبات معالجة كثيرة؛ code splitting التفصيلي غير مثبت من المراجعة | تقسيم حسب route/tool وتأجيل محركات FFmpeg وPDF/OCR وOffice حتى الحاجة |
| CSS | stylesheet مركزي مع ملفات CSS خاصة بعدة مكونات | قياس الحجم بعد build وإزالة غير المستخدم دون المساس بالهوية البصرية |
| الصور | لا توجد مكتبة صور كبيرة واضحة؛ الشعار/favicon صغير | التأكد من `alt` للصور الفعلية واستخدام أبعاد ثابتة عند وجود صور |
| lazy loading | لا يظهر نمط واسع يمكن تقييمه من الصور لأن الواجهة تعتمد غالبًا على CSS/icons | تطبيقه فقط على الصور/المحتوى غير الحرج عند الحاجة |
| الموارد blocking | CSS وentry JS أساسيان في SPA | قياس Lighthouse/PageSpeed على الدومين النهائي قبل وبعد أي تغيير |
| LCP/CLS/INP | لم تُقَس بأداة متصفح مستقرة في هذه الجلسة | جمع RUM وCrUX/Lighthouse في بيئة مستقرة؛ لا يمكن استنتاج أرقام موثوقة من curl |
| caching | robots/sitemap لهما cache قصير؛ HTML النهائي يعرض no-cache في الفحص | إبقاء HTML آمنًا للتحديث، وتثبيت caching للأصول ذات hash |

لا ينبغي تغيير محركات معالجة الملفات كجزء من SEO. أي تحسين أداء يجب أن يكون route-level loading أو ترتيب موارد أو metadata، مع اختبار عدم تأثيره على وظائف الأدوات.

## 7. Content وSearch Intent

| مجموعة الأدوات | نية البحث الأساسية | كلمات عربية مقترحة | كلمات إنجليزية ثانوية | المحتوى الناقص عادةً |
|---|---|---|---|---|
| PDF | إنجاز إجراء محدد على PDF | دمج PDF، ضغط PDF، تقسيم PDF، تحويل PDF إلى Word، حماية PDF | merge/compress/split PDF, PDF to Word | شرح المدخلات، الخصوصية، القيود، أمثلة الاستخدام، روابط الأدوات ذات العلاقة |
| الصور | ضغط/تحويل/تغيير أبعاد | ضغط الصور، تحويل JPG إلى PNG، تغيير حجم الصورة | compress/convert/resize image | مقارنة الجودة والحجم، صيغ الإخراج، حدود المتصفح |
| Word والمستندات | تحويل أو حماية مستند | تحويل Word إلى PDF، حماية ملف Word، TXT إلى DOCX | Word to PDF, protect DOCX | شرح الحفاظ على التخطيط، حالات عدم التطابق، الحماية وتسجيل الدخول |
| Excel | تحويل أو تصدير أو دمج جداول | تحويل Excel إلى PDF، Excel إلى CSV، دمج ملفات Excel | Excel to PDF/CSV, merge Excel | أمثلة للأوراق المتعددة، الصيغ المدعومة، حدود الجداول |
| OCR | استخراج نص من صورة أو PDF ممسوح | OCR عربي، استخراج النص من صورة، تحويل صورة إلى نص | Arabic OCR, image to text | أمثلة عربية، جودة النص، الجداول، اللغات والقيود |
| QR | إنشاء أو قراءة رمز | إنشاء QR، قراءة QR من صورة، QR للواي فاي | QR generator/reader | حالات الاستخدام، الخصوصية، أنواع المحتوى، شرح المعاينة |
| توقيع وفحص الملف | إجراء عملي متخصص | توقيع PDF، فحص SHA-256، بصمة الملف | sign PDF, file hash/checksum | توضيح أن التوقيع بصري وليس توقيعًا رقميًا معتمدًا، وأمثلة checksum |
| الصوت والفيديو | تحويل أو قص أو استخراج معلومات | تحويل الصوت، قص الفيديو، معلومات الملف | convert/trim audio/video, metadata | توضيح الجاهزية والتجريبية، المتصفحات المدعومة، أحجام الملفات |

الصفحات الحالية قادرة على تلبية نية الاستخدام المباشر، لكنها ليست بعدُ صفحات منافسة قوية لنتائج البحث لأنها تعتمد على عنوان ووصف قصيرين وواجهة رفع، دون نص إرشادي عميق أو أسئلة مخصصة أو أمثلة. ينبغي تجنب keyword stuffing؛ كل صفحة يجب أن تستهدف نية واحدة، وتستخدم كلماتها في title وH1 والوصف والنص المفيد فقط.

## 8. صفحات SEO مستقلة مقترحة

الأولوية ليست إنشاء نسخ جديدة من الأدوات، بل تقوية صفحات الأدوات الموجودة. المرشحون الأوائل هم ضغط PDF، دمج PDF، PDF إلى Word، OCR، ضغط الصور، تحويل الصور، Word إلى PDF، Excel إلى PDF، إنشاء QR، وتوقيع PDF. هذه الصفحات لها نية بحث واضحة وقيمة عملية، ويمكن إثراؤها داخل نفس route ونفس محرك الأداة.

بعدها تأتي صفحات التصنيف `/tools/pdf` و`/tools/images` و`/tools/documents` و`/tools/word` و`/tools/excel` كـlanding pages تجمع الأدوات دون تكرار محرك المعالجة. صفحات الصوت والفيديو لا ينبغي دفعها SEO قبل تثبيت حالات الجاهزية والرسائل الخاصة بالأدوات التجريبية.

## 9. Google Discovery وMonitoring

يحتاج الإطلاق إلى إنشاء أو تأكيد property للنطاق النهائي `waselfile.com` في Google Search Console، وتقديم sitemap النهائي من نفس النطاق، ثم استخدام URL Inspection لصفحة Home وفهرس الأدوات وعدد من الأدوات والتصنيفات. ينبغي متابعة Coverage وPage indexing وCrawl stats وEnhancements وCore Web Vitals، مع فحص Rich Results للـJSON-LD.

لا ينبغي اعتبار sitemap وحده دليلًا على الفهرسة؛ Google تصفه بأنه hint، بينما تعتمد الأولوية أيضًا على الروابط الداخلية، جودة المحتوى، canonical، وحالة HTTP [1].

## 10. العربية والإنجليزية

الواجهة تستخدم locale داخل نفس المسارات، وتضم نصوصًا عربية وإنجليزية في المكونات. هذا مناسب لتجربة المنتج لكنه لا يعطي Google URLs منفصلة للغتين، ولا يظهر `hreflang`. إذا كان الهدف منافسة بحث عربي وإنجليزي فعليًا، فيجب اتخاذ قرار معماري قبل التنفيذ: إما صفحات locale مستقلة مثل `/ar/...` و`/en/...` مع metadata وcanonical وhreflang متبادلة، أو إبقاء نفس URL بلغة افتراضية واحدة وعدم إضافة hreflang غير قابل للتنفيذ.

لا أوصي بإضافة hreflang لمجرد وجود زر تبديل لغة داخل SPA. يجب أن تكون لكل alternate نسخة URL قابلة للزحف ومحتواها الأساسي مترجمًا، لا أن يختلف header/footer فقط. كما يجب ألا تُعرض نسخة عربية ونسخة إنجليزية متطابقة تقريبًا مع canonical ملتبس.

## 11. Security وSafety

خطة SEO المقترحة لا تحتاج إلى تعديل قاعدة البيانات أو Catalog أو Registry أو API أو صلاحيات User/Admin أو محركات معالجة الملفات. يجب أن تبقى صفحات تسجيل الدخول والحساب والإدارة خارج sitemap، ويفضل تحديد `noindex` لها إذا كانت قابلة للوصول العام. يجب عدم تضمين محتوى ملفات المستخدمين أو metadata حساسة في HTML أو structured data أو logs.

تظل أدوات Office ذات المعالجة الخادمية بحاجة إلى الحفاظ على حارس تسجيل الدخول، وتظل الأدوات المحلية بحاجة إلى إبقاء الملفات داخل المتصفح حيثما هو متوقع. SEO لا يبرر جعل endpoint معالجة أو صفحة إدارية قابلة للفهرسة.

## 12. Competitive SEO

المنافسة مع مواقع أدوات الملفات المعروفة تحتاج إلى صفحات إجراء متخصصة ذات شرح واضح للخصوصية، الصيغ، الخطوات، القيود، الأسئلة الشائعة، وروابط الأدوات المرتبطة. نقطة تميز Wasl هي العربية أولًا والمعالجة المحلية؛ ينبغي تحويل ذلك إلى محتوى إثباتي دقيق لا إلى وعود مطلقة. يجب توضيح متى تكون المعالجة محلية ومتى تكون خادمية مؤقتة، مع عدم الادعاء بتطابق كامل للتخطيط أو توقيع رقمي معتمد إذا لم يكن ذلك صحيحًا.

المحتوى المقترح للمنافسة هو شروحات أصلية، جداول صيغ مدعومة، أمثلة استخدام عربية، صفحات مساعدة قصيرة، وقيود صريحة. لا توجد حاجة لنسخ نصوص المنافسين أو إضافة مراجعات وتقييمات غير موثقة.

## 13. خطة الأولوية

| الأولوية | الإجراء | الملف/المكان المتوقع | الأثر المتوقع | تأثير الوظيفة |
|---|---|---|---|---|
| P0 | توحيد النطاق canonical إلى `https://waselfile.com` في HTML والخادم | `client/index.html`, `server/seoRoutes.ts`, إعدادات serving | إزالة تضارب النطاق ورفع وضوح النسخة الأساسية | لا يغير محركات الملفات؛ قد يغير metadata فقط |
| P0 | إعادة بناء sitemap ديناميكيًا من الصفحات العامة النشطة فقط، وإضافة `/tools` والتصنيفات والأدوات الجاهزة | `server/seoRoutes.ts` ومصدر catalog المسموح | رفع اكتشاف وتغطية الصفحات المهمة | لا يغير الكتالوج؛ يقرأ حالته فقط |
| P0 | إضافة سياسة robots للنطاق النهائي واستبعاد المسارات الحساسة عند الحاجة | `server/seoRoutes.ts` | منع فهرسة الحساب والإدارة والصفحات غير الجاهزة | لا يغير الصلاحيات أو الوصول الوظيفي |
| P0 | معالجة soft 404: 404 حقيقية للمسارات غير الموجودة أو `noindex` واضح للصفحة الخطأ | `server/_core/vite.ts`, routing/error handling | تقليل صفحات 200 الوهمية وتحسين crawl quality | يحتاج اختبار routes فقط |
| P1 | metadata route-aware لفهرس الأدوات والتصنيفات والصفحات الثابتة | صفحات `ToolsIndexPage`, `ToolCategoryPage`, `StaticPage` أو طبقة metadata مشتركة | تحسين CTR والملاءمة لكل نية بحث | لا يغير واجهة الأداة أو المعالجة |
| P1 | توسيع metadata للأدوات مع OG/Twitter و`og:url` وصورة اجتماعية حقيقية إن توفرت | `ToolPage.tsx` وطبقة head | تحسين snippets والمشاركة الاجتماعية | لا يغير processing |
| P1 | تقوية نصوص صفحات الأدوات والتصنيفات مع أسئلة وأمثلة وقيود مخصصة | مكونات صفحات الأدوات والتصنيفات، دون نسخ الأدوات | زيادة topical relevance ورضا المستخدم | تغيير محتوى فقط |
| P1 | تحسين الربط الداخلي والbreadcrumbs والفوتر | `Home.tsx`, `PublicLayout.tsx`, صفحات الأدوات | توزيع authority وتقليل orphan risk | لا يغير محركات المعالجة |
| P1 | إعداد Search Console وURL Inspection ورفع sitemap النهائي | Google Search Console | مراقبة الفهرسة والأخطاء فعليًا | خارجي، لا يغير التطبيق |
| P2 | تقسيم JavaScript وتحميل محركات PDF/OCR/media/Office عند الحاجة | imports وroute boundaries | خفض JS الأولي وتحسين LCP/INP | يحتاج اختبارات وظيفية لكل أداة |
| P2 | إضافة Schema مكتمل ومتطابق: WebSite/Organization/BreadcrumbList/ItemList عند مناسبة كل صفحة | طبقة structured data مشتركة | فهم أفضل للصفحات وفرص rich results | لا يغير الوظائف، لكن يحتاج تحققًا صارمًا |
| P2 | قياس Lighthouse وCWV وRUM على Home وTools و3–5 أدوات | CI/monitoring/جهاز اختبار | أرقام baseline قابلة للمقارنة | لا يغير المنتج بحد ذاته |
| P3 | قرار معماري للـlocale URLs وhreflang | routing وmetadata وsitemap | توسع بحث عربي/إنجليزي منضبط | قد يغير URL architecture، لذا يحتاج موافقة مستقلة |
| P3 | إنشاء مركز مساعدة ومقالات أصلية مرتبطة بالأدوات | صفحات محتوى جديدة | نمو long-tail والربط الداخلي | محتوى فقط إذا بقي منفصلًا عن محركات المعالجة |

## النتيجة النهائية

**حالة SEO الحالية: قابلة للتحسين وليست جاهزة بعد لإطلاق SEO واسع.** الأساس الوظيفي والروابط الدلالية جيدان، لكن تضارب النطاق في `robots.txt` و`sitemap.xml` وcanonical، ونقص تغطية sitemap، وغياب metadata المخصصة للتصنيفات، واحتمال soft 404 هي عناصر يجب حسمها قبل الاستثمار في صفحات ومحتوى إضافي.

لم يتم تنفيذ أي تعديل في كود التطبيق أو قاعدة البيانات أو الكتالوج أو Registry أو API أو الصلاحيات أو محركات المعالجة. تعذر الفحص البصري بمتصفح Chromium في هذه الجلسة لأن بيئة المتصفح أبلغت أن المتصفح غير متاح؛ لذلك لا توجد قياسات موثوقة لـLCP/CLS/INP أو computed styles في هذا التقرير. تم الاعتماد بدلًا من ذلك على قراءة المصدر وفحص HTTP/HTML المنشور قراءة فقط.

## ملاحظة عن حالة شجرة المشروع

ظهر `todo.md` كملف متغير محليًا في `git status` أثناء التدقيق. لم تُجرَ أي عملية Git أو نشر، لكن هذا الملف يجب استبعاده من أي تنفيذ لاحق ما لم يصدر أمر مستقل بخلاف ذلك.

## المراجع

[1]: https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap "Google Search Central: Build and submit a sitemap"

[2]: https://developers.google.com/search/docs/crawling-indexing/canonicalization "Google Search Central: What is canonicalization"

[3]: https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics "Google Search Central: Understand JavaScript SEO basics"

[4]: https://developers.google.com/search/docs/appearance/structured-data/intro-structured-data "Google Search Central: Introduction to structured data markup in Google Search"
